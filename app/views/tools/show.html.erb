 <h1><%= @tool.name %></h1>
  <p>Tool ID: <%= @tool.id %></p>
  <p>Is the tool in working conditon? <%= @tool.working_condition %></p>
  <p>How many? <%= @tool.quantity %></p>
  <p>Created at: <%= @tool.created_at %></p>
  <p>Updated at: <%= @tool.updated_at %></p>
  <p>Toolbox ID: <%= @tool.toolbox_id %></p>

  <%= link_to "Update Tool", "/tools/#{@tool.id}/edit" %>